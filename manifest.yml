applications:
  - name: sap-aicore-proxy
    memory: 256M
    instances: 1
    buildpacks:
      - python_buildpack
    command: uvicorn main:app --host 0.0.0.0 --port $PORT
    env:
      # 필요한 경우 여기에 환경변수를 추가할 수 있습니다.
      # 하지만 보통 BTP에서 Service Binding을 하거나 JSON을 사용합니다.
      LOG_LEVEL: INFO
